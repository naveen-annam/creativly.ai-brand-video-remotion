// Entry file — render with:
// npx remotion render src/index.ts CreativlyBrandVideo out/brand-video.mp4

import { registerRoot } from "remotion";
import { RemotionRoot } from "./Root";

registerRoot(RemotionRoot);
